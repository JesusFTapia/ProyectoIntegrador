<?xml version="1.0" encoding="UTF-8"?>

<?import java.lang.*?>
<?import java.util.*?>
<?import javafx.scene.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.effect.DropShadow?>
<AnchorPane prefHeight="400.0" prefWidth="600.0"
            style="-fx-background-color: linear-gradient(to bottom, #f5f5f5, #e8e8e8);"
            xmlns="http://javafx.com/javafx/17.0.12"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.moxxdesignsfront.controllers.vistaTrabajosController">

    <VBox alignment="TOP_CENTER" spacing="30" AnchorPane.topAnchor="0" AnchorPane.bottomAnchor="0"
          AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0">

        <!-- Título -->
        <Label text="Gestión de Trabajos"
               style="-fx-font-size: 32px;
                      -fx-font-weight: bold;
                      -fx-text-fill: #9C7277;
                      -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 2, 0, 0, 1);">
            <VBox.margin>
                <Insets top="40"/>
            </VBox.margin>
        </Label>

        <!-- Botón Crear Trabajo -->
        <Button text="✨ Crear Trabajo"
                prefWidth="250.0"
                prefHeight="60.0"
                onAction="#cargarVistaUno"
                style="-fx-background-color: linear-gradient(to bottom, #9C7277, #8B6569);
                       -fx-text-fill: white;
                       -fx-background-radius: 15;
                       -fx-font-size: 18px;
                       -fx-font-weight: bold;
                       -fx-cursor: hand;
                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 3);">
        </Button>

        <!-- Botón Ver Trabajo -->
        <Button text="👁 Ver Trabajos"
                prefWidth="250.0"
                prefHeight="60.0"
                onAction="#cargarVistaDos"
                style="-fx-background-color: linear-gradient(to bottom, #9C7277, #8B6569);
                       -fx-text-fill: white;
                       -fx-background-radius: 15;
                       -fx-font-size: 18px;
                       -fx-font-weight: bold;
                       -fx-cursor: hand;
                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 3);">
        </Button>

        <VBox.margin>
            <Insets bottom="40"/>
        </VBox.margin>
    </VBox>

    <!-- Efecto de sombra para toda la ventana -->
    <effect>
        <DropShadow color="rgba(0,0,0,0.05)" radius="20" spread="0.2"/>
    </effect>
</AnchorPane>
